#include <stdio.h>
/**
 * main - entry point
 *
 * Description: alphabet
 *
 * Return: 0 (success)
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
			putchar  (ch);
			ch++;
	}
	return (0);
}
