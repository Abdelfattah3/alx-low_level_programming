#include "main.h"

/**
* mul - checks if the alphabet is uppercase or not
* @a: input character
* @b: input character
* Return: 1 if uppercase 0 if lowercase
*/

int mul(int a, int b)
{
	int res;
	
	res = a * b;
	return (res);
}

