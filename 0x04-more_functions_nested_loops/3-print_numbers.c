#include "main.h"

/**
* print_numbers - checks if the alphabet is uppercase or not
* Return: 1 if uppercase 0 if lowercase
*/

void print_numbers(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	_putchar(i);
	_putchar('\n');

}

